<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".views.BaseActivity">

    <TextView
        android:id="@+id/text_service_button"
        android:layout_marginTop="40dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="TEXT SERVICE"
        android:textColor="@android:color/holo_red_dark"
        android:background="@android:color/holo_green_light"/>

    <TextView
        android:layout_below="@+id/text_service_button"
        android:id="@+id/image_service_button"
        android:layout_marginTop="40dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="IMAGE SERVICE"
        android:textColor="@android:color/holo_red_dark"
        android:background="@android:color/holo_green_light"/>

    <Button
        android:id="@+id/switch_button"
        android:layout_marginTop="40dp"
        android:layout_below="@id/image_service_button"
        android:text="switch implementation"
        android:layout_width="wrap_content"
        android:layout_height="50dp"/>
</RelativeLayout>
